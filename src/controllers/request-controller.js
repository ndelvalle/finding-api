import Controller from '../libraries/controller';
import RequestModel from '../models/missing-model';

class RequestController extends Controller {}

export default new RequestController(RequestModel);
